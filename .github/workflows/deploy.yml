# AWS ECS/ECR workflow kept for future reference.
# Uncomment this file if you want to switch back from Modal deployment.
#
# name: Deploy to Amazon ECR
#
# on:
#   push:
#     branches: [ "main" ]
#
# env:
#   AWS_REGION: ap-southeast-2
#   ECR_REPOSITORY: portfolio/cv-api
#   AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
#   ECS_CLUSTER: portfolio
#   ECS_SERVICE: cv-api
#   # Add the container name (usually matches the service name)
#   CONTAINER_NAME: cv-api
#
# permissions:
#   id-token: write # Required for OIDC
#   contents: read
#
# jobs:
#   deploy:
#     name: Deploy
#     runs-on: ubuntu-latest
#
#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4
#
#     - name: Configure AWS credentials
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         role-to-assume: ${{ env.AWS_ROLE_ARN }}
#         aws-region: ${{ env.AWS_REGION }}
#
#     - name: Login to Amazon ECR
#       id: login-ecr
#       uses: aws-actions/amazon-ecr-login@v2
#
#     - name: Build, tag, and push image to Amazon ECR
#       id: build-image
#       env:
#         ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#         IMAGE_TAG: ${{ github.sha }}
#       run: |
#         docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
#         docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
#         # Output the image URI for the next step
#         echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
#
#     # NEW STEP 1: Update the image URI in the task definition JSON
#     - name: Fill in the new image ID in the Amazon ECS task definition
#       id: task-def
#       uses: aws-actions/amazon-ecs-render-task-definition@v1
#       with:
#         task-definition: task-definition.json
#         container-name: ${{ env.CONTAINER_NAME }}
#         image: ${{ steps.build-image.outputs.image }}
#
#     # NEW STEP 2: Deploy the updated task definition to the service
#     - name: Deploy Amazon ECS task definition
#       uses: aws-actions/amazon-ecs-deploy-task-definition@v1
#       with:
#         task-definition: ${{ steps.task-def.outputs.task-definition }}
#         service: ${{ env.ECS_SERVICE }}
#         cluster: ${{ env.ECS_CLUSTER }}
#         wait-for-service-stability: true